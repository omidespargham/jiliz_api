from django.urls import path
from . import views

app_name = 'install_repair_v1'

urlpatterns = [
    # path('install_advert_make/', views.InstallRepairMakeView.as_view(), name='InstallAdvertMake'),
]
